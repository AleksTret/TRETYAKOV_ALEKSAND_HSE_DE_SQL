# Домашнее задание 7

**Выполнил - Третьяков Александр Юрьевич**

## Упражение 1

### Задание
Предположим, что для какой-то таблицы создан уникальный индекс по двум
столбцам: `column1` и `column2`. В таблице есть строка, у которой значение атрибута `column1` равно `ABC`, а значение атрибута `column2` — `NULL`. Мы решили
добавить в таблицу еще одну строку с такими же значениями ключевых атрибутов, т. е. `column1` — `ABC`, а `column2` — `NULL`.
Как вы думаете, будет ли операция вставки новой строки успешной или завершится с ошибкой? Объясните ваше решение.

### Решение

Операция вставки будет успешной.

Причина успеха в принципе что: Два значения `NULL` не считаются равными друг другу. Таким образом требование уникальности индекса не нарушается.

Проверим это.

Создадим таблицу и добавим индекс
```sql
CREATE TABLE test_table (
    id SERIAL,
    column1 VARCHAR(10),
    column2 VARCHAR(10)
);

CREATE UNIQUE INDEX idx_unique_columns ON test_table (column1, column2);
```
<img src="./assets/ex_1/2025-11-15 202632.jpg" width="700">

Выполним вставки
 - Первую
```sql
INSERT INTO test_table (column1, column2) VALUES ('ABC', NULL);
```
- Вторую
```sql
INSERT INTO test_table (column1, column2) VALUES ('ABC', NULL);
```
Обе вставки выполнены успешно.

Проверим результат
```sql
SELECT * FROM test_table;
```
<img src="./assets/ex_1/2025-11-15 203044.jpg" width="700">